To run application use simple ./run scripts (especially for running resizers, because it's necessary to run 3 resizers with proper parameters). 
Thumbnails are shown after moving mouse on image title on main page.

